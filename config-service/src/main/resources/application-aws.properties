# =============================================================================
# config-service - AWS profile (Phase 2 placeholder)
# =============================================================================
# Activated by `SPRING_PROFILES_ACTIVE=aws`. The profile-group in
# application.properties automatically also activates the `git` profile,
# which loads application-git.properties.
# =============================================================================

logging.level.root=WARN
logging.level.com.photoconnect=INFO
logging.level.org.springframework.cloud.config.server=INFO
