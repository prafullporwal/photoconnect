# =============================================================================
# config-service — AWS profile (Phase 2 placeholder)
# =============================================================================
# Activated by `SPRING_PROFILES_ACTIVE=aws`. The profile-group in
# application.yml automatically also activates the `git` profile, which
# causes application-git.yml to load the Git-backed configuration.
#
# This file holds AWS-environment-specific tweaks: tighter log levels,
# observability sampling rate, etc.
# =============================================================================

logging:
  level:
    root: WARN
    com.photoconnect: INFO
    org.springframework.cloud.config.server: INFO
