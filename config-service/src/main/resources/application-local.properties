# =============================================================================
# config-service - LOCAL profile
# =============================================================================
# Activated by `SPRING_PROFILES_ACTIVE=local` (or by default). The profile-
# group in application.properties automatically also activates the `native`
# profile, which loads application-native.properties and wires the
# NativeEnvironmentRepository.
# =============================================================================

logging.level.root=INFO
logging.level.org.springframework.cloud.config.server=INFO
logging.level.com.photoconnect=DEBUG
