# =============================================================================
# auth-service - LOCAL profile
# =============================================================================
# Local-only tweaks. Connection strings + JWT config + Eureka URL come from
# config-repo/auth-service.properties.
# =============================================================================

logging.level.root=INFO
logging.level.com.photoconnect=DEBUG
logging.level.org.hibernate.SQL=DEBUG
# Uncomment to see bind parameter values when debugging SQL
#logging.level.org.hibernate.orm.jdbc.bind=TRACE
